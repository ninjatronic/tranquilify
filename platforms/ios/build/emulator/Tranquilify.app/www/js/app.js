(function () {
    'use strict';

    angular
        .module('app', ['app.state', 'app.control', 'ngAnimate', 'ngTouch'])
        .config([function () {
        }])
        .run([function () {
        }]);
})();