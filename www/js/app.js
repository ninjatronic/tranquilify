(function () {
    'use strict';

    angular
        .module('app', ['app.state', 'app.control', 'ngAnimate'])
        .config([function () {
        }])
        .run([function () {
        }]);
})();